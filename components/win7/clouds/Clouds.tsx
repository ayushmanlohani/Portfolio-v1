"use client";

import { UnitwiseLanding } from "@/components/win7/clouds/UnitwiseLanding";

/**
 * The Clouds window: a rebuild of the Unitwise marketing mockup Ayushman
 * supplied, with Aceternity UI's cloud shader
 * (https://ui.aceternity.com/registry/cloud-shader-demo.json) retextured
 * into a warm backdrop behind the header instead of filling the window on
 * its own.
 */
export function Clouds() {
  return <UnitwiseLanding />;
}
